#ifndef _MY_PUBLIC_
#define _MY_PUBLIC_

#include <iostream>
#include <Windows.h>    // system() 함수 사용
#include <conio.h>  // gotoxy()) 함수 사용
#include <time.h>
#include <fstream>
#include <string>
#include <stack>
#include <queue>    // STL priority_queue(우선순위 큐) 사용
#include <locale>

using namespace std;

#endif